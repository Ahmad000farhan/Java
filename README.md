# RAG Chatbot Project

This project implements a Retrieval-Augmented Generation (RAG) chatbot.

## Setup Instructions
1. Clone the repository.
2. Compile the Java files in the `src` directory.
3. Run the `Chatbot` class to start the chatbot.